from setuptools import setup, find_packages

setup(
    name="causal-metric-trees",
    version="0.1.0",
    description="A library for building, visualizing, and analyzing Causal Metric Trees with AI-powered assistance.",
    author="Your Name",
    packages=find_packages(),
    install_requires=["pandas", "transformers"],
)
